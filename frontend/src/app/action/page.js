import { cn } from '../lib/utils';
import Link from 'next/link';

export const ActionPage = ({ className, title, description, header, icon, onOpen }) => {

    return (
      <div>

      </div>
    );
};
